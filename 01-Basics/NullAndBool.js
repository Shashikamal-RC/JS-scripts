//Assume temp is coming from Website API

// var temp = 0;

//done some calculation
//temp = 2
// if we don't get temp by some error
var temp
//now it is undefined
// NULL and UNDEFINED are NOT same

console.log("Current temp is : " + temp);